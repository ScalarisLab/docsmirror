/**
 * The one name the client's configuration lives under. Every reader of a
 * `docsmirror.*` setting imports it from here, so the section can never be
 * spelled two ways.
 * @docs server.md#settings
 */

export const SETTINGS_SECTION = 'docsmirror';
